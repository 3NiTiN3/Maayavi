function sendQuestion() {
    const userInput = document.getElementById('user_input').value;
    
    // TODO: Implement sending the question to your Python backend
    //       and updating the 'maayavi_response' div with the answer
}
